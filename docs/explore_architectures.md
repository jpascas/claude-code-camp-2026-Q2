1. An agent file with referenced files eg. AGENT.md, @~/docs/\*.MD

## Observations

- The coding harness created temp bash and then python script to manage the socket connection.
- I t made mistakes on the login sequence, including putting the username as Helloworld (the password), it required several attempts and apparently change on timing the commands, but it eventually succeeded. Ideally this should be solved in a deterministic way instead of wasting tokens just by try and error.
- Both Haiku 4.5 and Sonnet 5 were able to login successfully (after Haiku few failures)
- It did not understand at the beginning how to enter the menu options to begin playing, the game responded “That's not a menu choice!”, but it figure it out.
- with Haiku 4.5 it started to iterate and navigate the rooms but it was taking too long, it appeared that it created a script for each room, but it was progressing. However, I switched to Sonnet 5 and asked it the same and it change its approach: **“Let me explore more areas systematically in a single session”**. It appears it did it more efficiently.
- Sonnet 5 was able to find the bakery and list:
  > “I found the bakery and its menu.
  > Location: The Bakery — north of Main Street, which is west of Market Square in Midgaard (path: Temple → Temple Square → Market Square → Main Street → north).”
  > List the bakery menu:
  > Menu:
  > Item Cost Availability
  > A danish pastry 7 Unlimited
  > A bread 14 Unlimited
  > A waybread 73 Unlimited
- The room and map details are saved correctly to data/world.md, and current player state to data/player.md.
- It appears that Sonnet and Haiku were able to achieve the goal to find the bakery and list the menu, but Sonnet did it faster in a more efficient way.

## Technical Conclusion

- Even when the coding harness was able to complete the task, regarding efficiency A coding harness alone might not be a good fit for operating the MUD. Possible there are other ways to accomplish it faster, more reliably and using less tokens.

- The deterministic login, handling menu options, and connection flow should be handled by a reusable MUD SDK rather than regenerated by the model on every attempt.

- A specialized agent loop, potentially using an MCP server connected to the MUD SDK, would provide better reliability, visibility, and token
  efficiency.
